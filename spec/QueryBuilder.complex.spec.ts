import Model from '../src/model/Model';

xdescribe('QueryBuilder Complex', async () => {
    class User extends Model {
        name!: string;
        password!: string;
    }

    xit('should be ', async () => {
        expect(true).toBeTrue();
    });
});